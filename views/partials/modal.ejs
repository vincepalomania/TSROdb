<div id="actionModal" class="modal">
    <div class="modal-content">
      <span class="close" onclick="closeModal()">&times;</span>
      <h3>What do you want to do?</h3>
      <div class="modal-actions">
        <button id="updateBtn">Update</button>
        <button onclick="closeModal()">Cancel</button>
      </div>
    </div>
  </div>

  <div id="sellModal" class="modal">
    <div class="modal-content">
      <span class="close" onclick="closeSellModal()">&times;</span>
      <h3>Sell Fuel</h3>
      <form id="sellForm">
        <input type="hidden" id="sellFuelId" />
        <input type="hidden" id="sellPrice" />
  
        <label for="quantity">Quantity Sold (liters):</label>
        <input type="number" id="sellQuantity" required step="0.01" min="0.01" />
  
        <label for="machine_id">Machine ID (optional):</label>
        <input type="number" id="machineId" />
  
        <button type="submit">Sell</button>
      </form>
    </div>
  </div>
  
  <style>
    
    #sellModal input, #sellModal button {
      margin-top: 10px;
      width: 100%;
    }
  
    .modal {
      display: none; 
      position: fixed; 
      z-index: 999;
      left: 0; top: 0;
      width: 100%; height: 100%;
      overflow: auto;
      background-color: rgba(0,0,0,0.5);
    }
  
    .modal-content {
      background-color: #fff;
      margin: 10% auto;
      padding: 20px;
      width: 300px;
      border-radius: 8px;
      text-align: center;
      position: relative;
    }
  
    .modal-actions button {
      margin: 10px;
      padding: 8px 14px;
      cursor: pointer;
    }
  
    .close {
      position: absolute;
      top: 8px;
      right: 12px;
      font-size: 22px;
      cursor: pointer;
    }
  
    .content-card {
      cursor: pointer;
      transition: 0.2s;
    }
  
    .content-card:hover {
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
    }
  </style>
  