<div id="actionModal" class="modal">
    <div class="modal-content">
      <span class="close" onclick="closeModal()">&times;</span>
      <h3>What do you want to do?</h3>
      <div class="modal-actions">
        <button id="updateBtn">Update</button>
        <button onclick="closeModal()">Cancel</button>
      </div>
    </div>
</div>

<div id="sellProductModal" class="modal">
    <div class="modal-content">
      <span class="close" onclick="closeSellProductModal()">&times;</span>
      <h3>Sell Product</h3>
      <form id="sellProductForm">
        <input type="hidden" id="sellProductId" />
        <input type="hidden" id="sellProductPrice" />
  
        <label for="quantitySold">Quantity Sold:</label>
        <input type="number" id="sellProductQuantity" required step="0.01" min="0.01" />
  
        <button type="submit">Sell</button>
      </form>
    </div>
</div>
  
<style>
    
    #sellModal input, #sellModal button {
      margin-top: 10px;
      width: 100%;
    }
  
    .modal {
      display: none; 
      position: fixed; 
      z-index: 999;
      left: 0; top: 0;
      width: 100%; height: 100%;
      overflow: auto;
      background-color: rgba(0,0,0,0.5);
    }
  
    .modal-content {
      background-color: #fff;
      margin: 10% auto;
      padding: 20px;
      width: 300px;
      border-radius: 8px;
      text-align: center;
      position: relative;
    }
  
    .modal-actions button {
      margin: 10px;
      padding: 8px 14px;
      cursor: pointer;
    }
  
    .close {
      position: absolute;
      top: 8px;
      right: 12px;
      font-size: 22px;
      cursor: pointer;
    }
  
    .content-card {
      cursor: pointer;
      transition: 0.2s;
    }
  
    .content-card:hover {
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
    }
  </style>